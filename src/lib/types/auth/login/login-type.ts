export type TLoginProps = {
    email: string;
    password: string;
}

export type TLoginResponse = {
    success: boolean;
    message: string;
}

export type TLoginForm = {
    email: string;
    password: string;
}